const initialState = {
  isFetching: false,
  usersData: [],
  errors: null
};

export const userReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
