const initialState = {
  isFetching: false,
  postsData: [],
  errors: null
};

export const postReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
